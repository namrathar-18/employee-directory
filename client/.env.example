# Base URL of the API.
# Leave this empty for local development — Vite proxies /api to the Express server.
# In production set it to your deployed API origin, e.g. https://your-api.example.com
VITE_API_URL=
